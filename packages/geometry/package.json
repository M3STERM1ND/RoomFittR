{
  "name": "@roomfittr/geometry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo \"(geometry: no lint)\"",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"test/**/*.test.ts\"",
    "gen": "node scripts/generate-rules.mjs",
    "check": "node scripts/generate-rules.mjs --check"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
