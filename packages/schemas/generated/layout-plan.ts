/* GENERATED by packages/schemas/scripts/generate.mjs. Do not edit. */
/**
 * The AI's plan for a room, before any product is chosen or anything is placed (5.4 L2). This is the tool-schema Claude fills in. It is intent only: categories, priorities, budget shares and relations. Geometry is the solver's job, so nothing here carries a coordinate.
 */
export interface LayoutPlan {
  /**
   * Version of the schema set the document was written against.
   */
  schema_version: number;
  /**
   * User-selected or inferred. Drives the default budget ratios and the category vocabulary.
   */
  room_type: string;
  style?: string;
  /**
   * One furniture opportunity each. Capped at 10 in V1 (5.4 post-checks).
   *
   * @minItems 1
   * @maxItems 10
   */
  slots:
    | [
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        }
      ]
    | [
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        }
      ]
    | [
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        }
      ]
    | [
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        }
      ]
    | [
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        }
      ]
    | [
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        }
      ]
    | [
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        }
      ]
    | [
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        }
      ]
    | [
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        }
      ]
    | [
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        },
        {
          slot_id: string;
          /**
           * Must exist in the category vocabulary; checked deterministically after the call, never trusted.
           */
          category: string;
          /**
           * Drop order when the room or the budget runs out: nice first, must last.
           */
          priority: "must" | "should" | "nice";
          /**
           * Fraction of budget. Renormalised to 100% with a 5% reserve after the call, so the model's arithmetic is never load-bearing.
           */
          budget_share: number;
          /**
           * What the slot wants, in terms the solver's candidate generators understand. Every id referenced is checked against the RoomModel before use.
           */
          intent?: {
            /**
             * What this slot is positioned relative to: a wall, an opening, or another slot.
             */
            anchor?: {
              kind: "wall" | "opening" | "slot" | "region";
              ref: string;
            };
            /**
             * Closed list: these map one-to-one onto the solver's candidate-pose generators (5.4 L5.2). A relation the solver cannot generate poses for is worse than no relation at all.
             */
            relation?: "against_wall" | "in_front_of" | "under" | "around" | "beside" | "float";
            /**
             * What the item's front should point at.
             */
            facing?: string;
            /**
             * Free text for the solver's scoring hints and for explaining the plan. Never parsed as geometry.
             */
            notes?: string;
          };
        }
      ];
  /**
   * Short, shown to the user. Why this set of furniture for this room.
   */
  rationale?: string;
}
