/* GENERATED by packages/schemas/scripts/generate.mjs. Do not edit. */
/**
 * [width, depth, height], integer millimetres, all positive.
 *
 * @minItems 3
 * @maxItems 3
 */
export type Size3Mm = [number, number, number];

/**
 * Every object the segmentation stage found in the room, as gravity-aligned oriented bounding boxes (3.5 S5). Consumed by the viewer to draw ghosts of what was removed, and by the layout engine, which must treat anything the user kept as an obstacle.
 */
export interface ObjectsFile {
  /**
   * Version of the schema set the document was written against.
   */
  schema_version: number;
  pipeline_version: string;
  units: "mm";
  objects: {
    id: string;
    /**
     * The SAM 3 text prompt that matched, verbatim (3.5 S4).
     */
    label: string;
    /**
     * Which prompt group the label came from. This, not the label, decides `removable`.
     */
    label_group: "removable" | "structure" | "fixed";
    score: number;
    /**
     * Gravity-aligned oriented bounding box, trimmed to the 2nd-98th percentile of the instance's points to reject bleed.
     */
    obb: {
      /**
       * [x, y, z] in mm. Unlike floor geometry this carries height, so it is a 3-vector.
       *
       * @minItems 3
       * @maxItems 3
       */
      center: [number, number, number];
      size: Size3Mm;
      /**
       * Degrees about +Y. An item's local front is -Z before rotation (2.4).
       */
      yaw_deg: number;
    };
    /**
     * True for the removable prompt group. A `fixed` object that the user does not delete becomes a fixed_obstacle in the RoomModel.
     */
    removable: boolean;
    /**
     * SAM 3 video track this instance came from, kept for debugging a bad merge.
     */
    track_id?: number;
    /**
     * R2 key of the crop shown in the keep-or-remove UI.
     */
    thumb_ref?: string;
    point_count?: number;
  }[];
}
